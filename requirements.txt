trendspy>=0.0.5
pandas>=1.3.0
schedule>=1.1.0
backoff>=2.1.0
python-dotenv>=0.19.0
urllib3<2.0.0  # 使用1.x版本避免SSL警告
itchat-uos>=1.5.0.dev0  # 使用uos维护的版本，支持新版微信
tabulate>=0.9.0  # 用于格式化表格输出
PySocks>=1.7.1  # 支持 requests 走 socks5 代理
